---
title: Pagamento
---

## Criando um Pagamento para um Novo Lojista (Subconta)

Este guia descreve os passos para começar a aceitar pagamentos em cripto para um novo lojista usando a Bullring Finance.

> 💡 Nota: Você pode começar a criar solicitações de pagamento **imediatamente após o cadastro do lojista**. No entanto, o lojista deve concluir a verificação e registrar uma chave Pix antes que qualquer saque possa ocorrer. Veja o fluxo completo em [Saques](/pt/withdrawal).

---

### Etapa 1: Cadastrar um Novo Lojista

Envie uma requisição para o endpoint [`Criar Subconta`](/api-reference/subaccount/create-merchant) para registrar o lojista pelo e-mail.

```http
POST /v1/ramp/subaccount
```

**Corpo:**
```json
{
  "email": "merchant@example.com"
}
```

**Resposta de Exemplo:**
```json
{
  "id": "47a79c3d-9a2b-4998-898b-8eb7fad01526",
  "email": "merchant@example.com"
}
```

---

### Etapa 2: Criar uma Solicitação de Pagamento

Uma vez que o lojista esteja cadastrado, você pode imediatamente começar a gerar faturas utilizando o endpoint [`Criar Pagamento`](/api-reference/payments/create-payment).

```http
POST /v1/ramp/subaccount/{id}/payments
```

**Corpo:**
```json
{
  "amount": 1000,
  "currency": "brl",
  "note": "Leite x 5"
}
```

Isso retorna uma fatura Lightning que seu cliente pode usar para concluir o pagamento.

---

### 🔄 Testar no Ambiente de Homologação

Use [regtestwallet.netlify.app](https://regtestwallet.netlify.app) para simular pagamentos no ambiente de testes.

Certifique-se de usar o cabeçalho `x-api-key` e enviar requisições para:

```http
https://staging-api.bullring.finance
```

Você agora está aceitando pagamentos em cripto 🚀

